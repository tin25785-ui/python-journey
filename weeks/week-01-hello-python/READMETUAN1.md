# Tuần 01: Hello Python — Những Bước Chân Đầu Tiên 🐍

> **"Mọi hành trình ngàn dặm đều bắt đầu từ một dòng code."**

## 🎯 Mục tiêu tuần này

Tuần này, chúng ta không chỉ học cú pháp, mà học cách "nói chuyện" với máy tính. Bạn sẽ làm chủ:

1. **Môi trường:** Cài đặt Python 3.x và VS Code (vũ khí của lập trình viên).
2. **Xuất dữ liệu:** Hiểu cách dùng hàm `print()` để hiển thị thông tin.
3. **Toán tử:** Biến Python thành máy tính thông minh với các phép tính nâng cao.
4. **Ghi chú:** Cách dùng `# comment` để code sạch và dễ đọc.

---

## 🛣️ Lộ trình thực hiện (Step-by-Step)

1. **Cài đặt:** Hoàn thành mục 2 trong `notes.md` (Add Python to PATH là bước quan trọng nhất).
2. **Đọc hiểu:** Xem qua `notes.md` về cách dùng `print()` và các phép toán `//`, `%`.
3. **Thực hành:** Mở thư mục `exercises/` và bắt đầu gõ từng file.
4. **Sáng tạo:** Tự làm Mini-Project để tạo dấu ấn cá nhân.

---

## 📖 Kiến thức cần nắm

*   **Hàm `print()`**: Hiển thị chuỗi (string) hoặc số.
*   **Toán tử quan trọng**:
    *   `/` : Chia ra số thập phân (VD: `5 / 2 = 2.5`).
    *   `//`: Chia lấy phần nguyên (VD: `5 // 2 = 2`).
    *   `%` : Chia lấy phần dư (VD: `5 % 2 = 1`).
*   **Ép kiểu**: Dùng `int()` hoặc `float()` khi nhận dữ liệu từ `input()`.

## ✍️ Bài tập

Mỗi bài tập được thiết kế để rèn luyện một kỹ năng riêng biệt:

1.  🐣 **`ex01_hello.py`**: Tập trung vào hàm `print()`. Hãy thử in nhiều dòng và dùng ký tự đặc biệt.
2.  🧮 **`ex02_calculator.py`**: Thực hành các phép toán. Hãy chú ý sự khác biệt giữa các loại phép chia.
3.  💬 **`ex03_input.py`**: Làm quen với `input()`. Nhớ rằng `input()` luôn trả về chữ (string), muốn tính toán phải chuyển sang số.

> 💡 **Mẹo:** Nếu gặp lỗi `SyntaxError`, hãy kiểm tra xem bạn có quên đóng ngoặc `)` hay thiếu dấu nháy `"` không.

## 🚀 Mini-Project: ASCII Art Generator

Sử dụng tất cả kiến thức về `print()` và `input()` để tạo một "tấm danh thiếp" kỹ thuật số. Đây là lúc để bạn thể hiện sự sáng tạo!

## 📚 Đọc thêm

| Nguồn | Chương | Link |
|:------|:-------|:-----|
| **Think Python** | Programming as a way of thinking | Đọc |
| **Python Crash Course** | Getting Started | Đọc |
| **Official Docs** | Python Tutorial cho người mới | Xem |

## ✅ Checklist cuối tuần

- [ ] **Setup:** Cài đặt Python 3.x (đã check `python --version` trong Terminal).
- [ ] **Editor:** Cài đặt VS Code và Extension Python của Microsoft.
- [ ] **Lý thuyết:** Phân biệt được `5 / 2`, `5 // 2` và `5 % 2`.
- [ ] **Thực hành:** Hoàn thành 3 file bài tập trong thư mục `exercises/`.
- [ ] **Sáng tạo:** Hoàn thành Mini-Project ASCII Art.
- [ ] **Ghi chú:** Biết cách dùng dấu `#` để giải thích các dòng code phức tạp.
- [ ] **GitHub:** Commit và Push toàn bộ bài tập lên repository của bạn.

---
*Chúc bạn có một tuần khởi đầu thật thuận lợi và tràn đầy hứng khởi!*

